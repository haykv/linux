#!/bin/bash
i=$1
file=$2
>|temp3
for w in $(cat $file); do
    val=$(P7.2 $i $w)
    echo "$val: $w" >> temp3
done
keys=$(cut -f1 -d" " temp3 | sort -nu)
for k in $keys; do
    words=$(egrep "^$k " temp3 | cut -f2 -d" " | sort | tr "\n" " ")
    echo "$k $words"
done
