#!/bin/awk -f

BEGIN { 
    for (i = 1; i < ARGC; i++) {
        max[i] = 0
    }
}
{ 
    for (i = 1; i <= NF; i++)
        if (length($i) > max[ARGIND]) {
            max[ARGIND] = length($i)
        }
}
END {
    for (i = 1; i < ARGC; i++) {
        printf "%s:%d\n", ARGV[i], max[i]
    }
}
